StickerSmach:
    is a react native app built using expo that allow users to upload a picture from their profile and edit the photo
    by adding a sticker from the sticker options available. 

libraries_use:
    react-native-getsture-handler: 
                a way to interact with the native platform's gesture response system
    react-native-reanimate:
                To animate between gesture states
    